  data-link-title="{{card.inner_text_content_button.title}}"
  data-link-url="{{card.inner_text_content_button.url}}"
  data-link-target="{{card.inner_text_content_button.target}}"
  data-card-type="{{ card.asset_type }}"
  data-video-embed="{{card.video_embed}}"
  data-poster="{{TimberImage(card.poster_image).src}}"
  data-has-sound="{{card.has_sound}}"
  data-show-inner-text="{{ card.show_inner_text_content }}"
  data-title="{{ card.inner_text_content_title }}"
  data-description="{{ card.inner_text_content_description }}"
  data-image-src="{{TimberImage(card.image).src}}"